# 🐢 Turtle Orientation — Паспорт Черепахи Сознания

TurtleOrientation — это "панцирь" протокола L-THREAD. Он показывает, куда сейчас развёрнута система во времени и по отношению к человеку. Если Паук (Orientation Web) — это сеть нитей, то Черепаха — это то, как эта сеть ориентирована в данный момент.

## 1. Что такое TurtleSnapshot

TurtleSnapshot — это лёгкий снимок состояния ориентации, который L-THREAD может отдавать клиентам.

Пример логической структуры (не обязательно 1:1 с реальным кодом):

```ts
interface TurtleSnapshot {
  mainTimeDirection: "past" | "now" | "future";
  mainSocialAxis: "self" | "family" | "world";
  timeWeaveDepthScore: number;   // 0..1
  focusMomentumScore: number;    // 0..1
  resonanceHint?: string;        // optional human-readable hint
  recommendedMode?: "explore" | "stabilize" | "commit";
  timestamp: string;             // ISO-8601
}
```

## 2. Откуда берутся эти значения

TurtleOrientation опирается на:

- **TimeWeave** — глубина и сложность линий времени (`timeWeaveDepthScore`).
- **Focus Momentum** — метрика "импульса внимания" (PR #57–58: how fast & stable focus is changing).
- **Temporal Orientation View** — агрегированный взгляд на past / now / future, self / family / world, текущие ветки и сценарии.

Черепаха не пересчитывает всё заново — она собирает и конденсирует эти слои в один компактный snapshot.

## 3. Как читать TurtleSnapshot

- **mainTimeDirection**
  - `past` — система сейчас переориентирована на анализ прошлого (ретроспектива, уроки, "подбор хвостов").
  - `now` — фокус на текущей стабилизации (grounding).
  - `future` — фокус на траекториях и возможностях (foresight / планирование).

- **mainSocialAxis**
  - `self` — акцент на личной траектории (self-regulation, личные выборы).
  - `family` — акцент на близком поле (связи, согласование).
  - `world` — акцент на внешнем поле (работа, проекты, мир).

- **timeWeaveDepthScore (0..1)**
  - `0–0.3` → `shallow` — короткая история, поверхность.
  - `0.3–0.7` → `layered` — несколько слоёв, уже есть сложность.
  - `0.7–1` → `deep` — длинные траектории, сильная инерция.

- **focusMomentumScore (0..1)**
  - `low` → внимание рассыпано / замедлено.
  - `medium` → стабильный рабочий тонус.
  - `high` → мощный импульс, высокая собранность.

- **recommendedMode**
  - `explore` — безопасно пробовать варианты, гулять по ветвям.
  - `stabilize` — лучше закрепиться, навести порядок, не дергать нити.
  - `commit` — окно для принятия решений и связывания веток.

## 4. Как используют TurtleOrientation разные уровни

**LTP-клиенты (браузер, сервисы):** запрашивают TurtleSnapshot, чтобы:
- выбирать UI-режим (мягкий / фокусный / обзорный);
- адаптировать интенсивность интеракций;
- подсвечивать пользователю, где он "находится" в своей карте времени.

**LIMINAL OS:** использует TurtleOrientation как:
- глобальный индикатор фазы: режим сна/восстановления, режим навигации, режим решения;
- вход для higher-level политик (когда будить слои, когда наоборот гасить активность).

**Протокол L-THREAD как "второй интернет":** TurtleSnapshot становится универсальным форматом ориентации, любая система, говорящая на LTP, может:
- объявить свою ориентацию (куда смотрит);
- согласовать её с другим узлом;
- отдать пользователю понятный, компактный образ.

## 5. Почему это важно

> Черепаха — это способ не потеряться в паутине.

- **Паук:** сложная сеть нитей, контекстов, ветвей.
- **TimeWeave:** множество линий времени, сценариев, ветвлений.
- **Fuzzy Routing:** умная маршрутизация по этим ветвям.

TurtleOrientation отвечает на вопросы:

- "куда сейчас развёрнуто внимание системы?"
- "мы сейчас больше в лечении прошлого, стабилизации настоящего или сборке будущего?"
- "на кого мы ориентируемся — на конкретного человека, семью, или поле мир-событий?"

Это интерфейс для человека и для других систем. Без Черепахи — только сложная математика. С Черепахой — простая, читаемая ориентация.

---

Дальше можно закрепить спецификацию в JSON Schema или оформить user journeys: как человек, глядя на HUD Черепахи, понимает "ага, сейчас лучше не принимать решение / сейчас как раз окно для прыжка".
