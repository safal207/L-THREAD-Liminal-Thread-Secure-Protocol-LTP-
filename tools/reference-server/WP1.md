# WP1 Reference Server

Implementation branch for #500.

The reference server is deliberately independent from all SDK implementations. The final branch will contain the server, protocol primitives, deterministic scenarios, evidence output, tests and CI. This marker is replaced by the complete implementation before review.
