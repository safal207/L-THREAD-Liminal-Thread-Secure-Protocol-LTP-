# WP2 Four-SDK Wire Interoperability

Implementation branch for #501.

The final branch will replace this marker with real JavaScript, Python, Rust and Elixir adapters, a shared scenario catalog, a machine-readable compatibility matrix, reviewer report and permanent CI.
